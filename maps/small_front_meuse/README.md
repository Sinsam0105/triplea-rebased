# Small Front: Meuse Corridor

Tactical relief map with roads, rivers, a marked initial front, and NATO battalion counters.

The package is generated from `tools/generate_small_front_meuse_map.py`. Install `maps/small_front_meuse.zip` as a normal TripleA downloaded map. The playable XML mirrors the smoke-test scenario so map assets and automated balance tests use the same rules.
